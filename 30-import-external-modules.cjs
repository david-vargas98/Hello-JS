//we can use the external modules ina normal js file, not in a module type file
const os = require("os")

console.log(os.platform())
console.log(os.arch())
console.log(os.totalmem())
console.log(os.freemem())
